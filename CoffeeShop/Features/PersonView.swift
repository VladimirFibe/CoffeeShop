import SwiftUI

struct PersonView: View {
    var body: some View {
        Text("Hello, person!")
    }
}

struct PersonView_Previews: PreviewProvider {
    static var previews: some View {
        PersonView()
    }
}
